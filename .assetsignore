# Served on purpose: index.html, assets/ (incl. arena_head.wasm), bench/,
# data/bench.json, skills/
# Everything else is repo plumbing — never ship it as public assets.
.git
README.md
wrangler.toml
scripts
.DS_Store
.wrangler
wasm-head
